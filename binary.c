// find is there a number 8 in the array

#include<stdio.h>

void binary(int a, int s[]);

int main()
{
    int a;
    scanf("%d", &a);
    int s[a];
    for(int i = 0; i < a; i++)
    {
        scanf("%d", &s);
    }
    binary( a, s[]);
}

void binary(int a, int s[])
{
    int middle;
    while(middle > 0)
    {
    
         for(int i = 0; i < middle; i++)
    }
}